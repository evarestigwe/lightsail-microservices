apiVersion: v1
kind: Service
metadata:
  name: {{ include "common.fullname" . }}
spec:
  selector:
    app: {{ include "common.name" . }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: 80
      protocol: TCP
